# 家電戦争の作業ルール

## GitHub向けの説明を継続して詳しく記載する

ユーザーの継続指示により、以降の更新でもGitHub上で内容・現状・変更理由が分かる説明を保つ。

- 機能、仕様、構成、素材、操作・起動方法に変更があれば、README・CHANGELOG・関連設計書の影響箇所を更新する。
- READMEには目的、実装済み内容、計画・未実装、構成、利用・開発手順、制約、関連資料へのリンクを実態に合わせて記載する。
- 更新履歴には具体的な変更、利用者への影響、検証結果と未検証項目を記載する。PRを作成する場合も、問題と変更後の動作・検証・制約が読み取れる説明にする。
- 機能一覧や画像説明を現状と一致させる。原画・モックアップを動作画面として紹介せず、未実装機能・公開URL・テスト成功・性能値を作らない。
- リポジトリ内リンクは相対パス。端末固有パス、認証情報、一時ファイルを公開対象に含めない。
- 毎回の文章量を目的にせず、第三者が変更と現在地を理解するのに必要な詳細を残す。

## バージョンと公開

- 初期コミットの `V0.01 開発開始` は履歴として維持する。ユーザー指定により、次の版は `V0.0.2` とする。
- 以降のコミット・プッシュ依頼では `versioned-commit-push` スキルを使い、最新の `V<major>.<minor>.<patch>` からpatchを1増やす。件名は版と差分全体の日本語要約。
- 本文書だけではコミット・プッシュ・公開の許可にならない。ユーザーの依頼範囲に従い、タグは別途依頼されたときに作成する。
- 強制プッシュや既存履歴の書き換えは行わない。

## グラフィックス

このプロジェクトの画像作成・編集では [.agents/skills/kaden-war-graphics/SKILL.md](.agents/skills/kaden-war-graphics/SKILL.md) を使う。承認済み原画を保持し、素材台帳に参照元・実際のプロンプト・確認結果・承認状態を記録する。
