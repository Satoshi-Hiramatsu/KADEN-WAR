# 更新履歴

リポジトリの更新履歴です。ゲーム製品版v1.0の完成・公開を示すものではありません。

## 未リリース — 2026-09-05 / S1〜S3ローカルデモ

対応: R01（製品設計・開発）、R02（財務・生産）、R03（販売チャネルの初期2種）、R06の一部（簡易競合）、R07の一部（入門シナリオSC01）。S4の競合AI・年代イベント、S5の保存は未実装のため、S1〜S3も完了扱いにしない。

- ローカルで入門シナリオSC01「暮らしの電化」を通しで遊べるようにした。研究予算→製品設計→開発→生産→価格・販路→発売→週次の売上→月次・年次決算までが1つの会社状態でつながり、240週後に累計売上・累計利益・ブランドの3条件で成否を判定する。
- 純TypeScriptの経営エンジンを追加。状態更新の入口は `applyCommand` と `advanceWeek` の2つで、乱数はシードと状態を保存できるxorshift32、進行の同一性は状態ハッシュで比較する。表示用の需要予測は乱数を消費しない。
- 単一の複式台帳から損益計算書・貸借対照表・キャッシュフロー計算書を導くようにした。生産費は在庫資産に計上し、販売時に移動平均原価で売上原価へ振り替える。設備は月末に120か月定額で償却する。
- 必須支払いに現金が足りない週は、状態を変えずに週送りを中断して資金不足を知らせる。そのまま進めると不足分が未払金として残り猶予を1週消費し、4週続くと敗北する。未払金は現金のある範囲で自動返済する。
- 担当別の5画面（社長室・研究所・工場・販売本部・経理部）を追加。価格は販売本部、生産量は工場、設計は研究所に編集元を一本化し、他画面は値と移動リンクだけを表示する。承認済み顔原画から6担当の顔を切り出して報告に添えた（個別スプライト化はS9）。
- 定義データを分離した。製品分類3種と年別需要、部品11種、研究課題8件、販路2種、競合3社、シナリオSC01。
- 旧S0の暦検証画面は、タイトル画面と開発用の接続確認に置き換えた。保存はまだないため、再読み込みするとタイトルへ戻る。

### 検証結果と制約

- 型検査・Lint、単体／統合テスト31件、Webビルド、Worker dry-runが成功。
- テストで確認した性質: 開始時と週送り後の資産＝負債＋純資産の一致、期首現金＋純キャッシュフロー＝期末現金、48週での年次決算が1回だけ、月次純損益の合計＝累計利益、同シード同操作での状態ハッシュ一致、在庫が負にならないこと、販売数が需要と在庫を超えないこと、未解禁部品・能力超過の生産・借入枠超過の拒否、資金不足での中断と猶予切れ敗北。
- SC01の自動試走（240週）で、初年度赤字から5年目黒字化までを再現し目標達成を確認。シードを変えても破綻せず、同じシードでは同じ結果になる。バランスの本調整はS4以降。
- Windows / Edge 152.0.4191.62のヘッドレスブラウザで、1280×900と360×800を検証。研究→設計→開発→生産→発売→売上→決算の画面操作、API成功／失敗表示、キーボード操作、再読み込みが成功。横はみ出しとページ例外なし。再現スクリプトは `scripts/smoke.mjs`。
- 未検証・未実装: 保存と再開、競合AIの意思決定、年代イベント、規格・特許・人事、長時間プレイの安定性と性能、モバイル実機、Safari/Firefox、GitHub上のCI実行、Cloudflareへの配信。

## V0.0.3 — 2026-09-05 / S0ローカル基盤

対応: R10（暦・基盤）、R11（ローカルWorkers/D1 PoC）、R12（画面雛形）。S0全体は実環境の検証配信などが残るため未完了。S1の会計・決算・乱数・決裁は未実装。

- React/Viteの起動画面を追加。1週・4週送り、初期化、API接続確認を操作可能にした。週送りは暦表示だけで、製品開発・販売・決算は進行しない。保存はなく再読込で初期化される。
- 純TypeScriptの暦定義と導出を分離し、月境界・48週の年境界・対象末年・不正値の拒否をテストした。
- Workersの疎通APIとD1初期化マイグレーションを追加。初期化済み200、未初期化503、未提供パス404、GET以外405を検証。セーブ・認証テーブルはまだ持たない。
- Node 24系の開発基盤、依存ロック、厳密型検査、Lint、Vitest、Webビルド、Worker dry-run、GitHub Actions設定を追加。公開・自動デプロイは行わない。
- ルールノートに方針A・対象年代・暦・会計・資金不足・SC01の開発用初期案を記録。README・実装計画・開発手順を現状に更新。承認済み顔原画は変更していない。

### 検証結果と制約

- 型検査・Lint、単体／D1統合テスト14件、Webビルド、Worker dry-runが成功。
- ローカルD1マイグレーションとVite→Workers→D1のHTTP疎通が成功。
- Windows / Edge 152.0.4191.62のヘッドレスブラウザで、1280×900と360×800を検証。週送り・初期化・API成功／失敗・キーボードEnter・再読込が成功、横はみ出しとページ例外なし。360px画像を目視確認。再現スクリプトは `scripts/smoke.mjs`。
- GitHub上のCI実行、Cloudflareへの配信、実機モバイル、他ブラウザ、ゲームの決算・保存・完走・性能は未検証。S0の残作業は [開発手順](docs/development.md) と [ルールノート](docs/rule-note.md) に記載。

## V0.0.2 — 2026-09-05

### GitHub向けドキュメント

- READMEを新規作成。企画の目的、6担当の専門パート、承認済み顔原画、資料一覧、現在の成果物と未実装項目、技術計画、ロードマップ、取得方法を記載。
- AGENTS.mdに、今後もREADME・更新履歴・関連仕様を詳しく更新する継続ルールと、専用画像制作スキルの参照を追加。
- ローカルの初期履歴を維持し、ユーザー指定の3桁バージョン形式へ移行。
- 一時ファイル、認証用環境ファイル、依存・ビルド出力を除外する.gitignoreを追加。

### 検証と現状

- リポジトリ内Markdownの相対リンクと、顔原画の1536×1024寸法を確認。
- 専用グラフィックス作成スキルの形式検証を実施。
- ゲーム本体、開発環境、CIは未実装。ビルド・ゲーム動作・端末別UI検証は未実施。

## V0.01 — 2026-09-05

既存の初期コミット `d2a33f1`（開発開始）。

- 家電戦争企画書、実装計画書、担当別ゲーム設計を収録。
- 経営者・設計・販売・経理・生産・人事の6担当の顔原画と制作台帳を収録。顔原画はユーザー承認済み。
- 同じテイストの画像を継続制作するプロジェクト専用スキルとプロンプト例を収録。
